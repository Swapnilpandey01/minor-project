# MERN Chat Server

## Setup
1. Install dependencies: `npm install`
2. Create a `.env` file (example provided)
3. Run: `npm run dev` (requires nodemon) or `npm start`

## .env example
MONGODB_URI=mongodb://localhost:27017/mern_chat
JWT_SECRET=your_jwt_secret_here
PORT=5000
